{% extends 'base.html.twig' %}

{% block body %}
    <h1>Side</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ side.id }}</td>
            </tr>
            <tr>
                <th>Sidename</th>
                <td>{{ side.sideName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('side_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('side_edit', { 'id': side.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
