{% extends 'layout.html.twig' %}

{% block body %}
        <h1>Ajout d'une direction</h1>

        <div class="container center">
            {{ form_start(form) }}
            {{ form_widget(form.sideName, {'attr': {'class': 'input-text-color'}} ) }}
            <input type="submit" value="Enregistrer" class="submit" />
            {{ form_end(form) }}

            <ul class="center">
                <li>
                    <a href="{{ path('side_index') }}"><h5>retour à la liste</h5></a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
